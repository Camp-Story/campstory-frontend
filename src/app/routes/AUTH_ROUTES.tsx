import { PATH } from "@constants/path";

const AUTH_ROUTES = [
  {
    path: PATH.signUp,
    element: <>signUp</>,
  },
  {
    path: PATH.login,
    element: <>login</>,
  },
];

export default AUTH_ROUTES;
